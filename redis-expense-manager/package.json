{
  "name": "redis-test",
  "version": "1.0.0",
  "description": "Test script for Redis connection",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node test-redis.js",
    "test:api": "node test/api.test.js",
    "lint": "eslint .",
    "redis:start": "cd redis && ./run-redis.sh",
    "redis:stop": "cd redis && ./cleanup-redis.sh",
    "redis:cleanup": "cd redis && ./cleanup-data.sh",
    "dev:full": "npm run redis:start && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "axios": "^1.8.4",
    "eslint": "^8.56.0",
    "nodemon": "^3.0.3"
  }
}
